{
  "groups": [
    {
      "title": "Introduction",
      "items": [
        {
          "id": "about",
          "label": "About ZYNQ Core",
          "icon": "home",
          "badge": "v7.5.28",
          "content": "# ZYNQ Core v7.5.28\n\n`ZYNQ Core` is a modern, lightweight, and powerful game library loader with intelligent device detection. It lets you dynamically load, search, filter, sort, randomize, and manage large collections of games with a clean and chainable API.\n\n### What's New in v7.5.28\n\n- New `shuffle()` method\n- New `sortBy(key)` and `sortByPopularity()` methods\n- Convenient `getRandomOne()` method\n- `getByAlias()` and `getByCategory()` for quick lookups\n- History system (`addToHistory`, `getHistory`, `clearHistory`)\n- Useful getters: `total` and `filteredCount`\n- Improved chaining and overall stability\n\n### Core Features\n\n* **Dynamic Game Loader** — Async fetching from JSON with thumbnail support\n* **Smart Device Detection** — Automatic detection for desktop, mobile, and tablet\n* **Fully Chainable API** — Fluent method chaining for complex queries\n* **Favorites System** — Persistent via LocalStorage\n* **Advanced Filtering & Sorting** — Search, category, supported devices, random, shuffle\n* **History Tracking** — Keep track of recently played or viewed games\n\n---\n\n### Installation\n\n#### ES Module (recommended)\n```html\n<script type=\"module\">\nimport 'https://cdn.jsdelivr.net/gh/un-zynq/v2/dev/lib7.5.28.min.js';\n</script>\n```\n\n#### Standard Script\n```html\n<script src=\"https://cdn.jsdelivr.net/gh/un-zynq/v2/dev/lib7.5.28.min.js\"></script>\n```"
        },
        {
          "id": "quickstart",
          "label": "Quick Start",
          "icon": "play-circle",
          "badge": "Start here",
          "content": "# Quick Start\n\n### 1. Initialize the library\n```javascript\nawait ZYNQ.init();\n```\n\n### 2. Get the current list\n```javascript\nconsole.log(ZYNQ.list);\n```\n\n### 3. Get one random game\n```javascript\nconst game = ZYNQ.random().list[0];\nconsole.log(game.name);\n```\n\n### 4. Get one random game (new shortcut)\n```javascript\nconst game = ZYNQ.getRandomOne();\nconsole.log(game.name);\n```"
        }
      ]
    },
    {
      "title": "Configuration",
      "items": [
        {
          "id": "init-options",
          "label": "Initialization Options",
          "icon": "settings",
          "content": "# Initialization Options\n\nYou can configure ZYNQ Core when calling `init()`.\n\n### Available Options\n\n| Option     | Type     | Default      | Description                                      |\n|------------|----------|--------------|--------------------------------------------------|\n| `mode`     | string   | `'all'`      | `'all'`, `'supported'`, or `'favs'`              |\n| `sort`     | string   | `'name'`     | Sort key: `'name'`, `'category'`, `'alias'`      |\n| `search`   | string   | `''`         | Initial search query                             |\n| `src`      | string   | (default)    | Custom JSON database URL                         |\n| `cdn`      | string   | (default)    | Custom thumbnail CDN base URL                    |\n\n### Example\n```javascript\nawait ZYNQ.init({\n  mode: 'supported',\n  sort: 'category',\n  search: 'mario',\n  src: 'https://your-site.com/games.json'\n});\n```"
        }
      ]
    },
    {
      "title": "Core API",
      "items": [
        {
          "id": "properties",
          "label": "Properties",
          "icon": "database",
          "content": "# Properties\n\n### Main Properties\n\n| Property            | Type     | Description                                              |\n|---------------------|----------|----------------------------------------------------------|\n| `ZYNQ.list`          | Array    | Current filtered / sorted / randomized game list         |\n| `ZYNQ.total`         | Number   | Total number of games in the library                     |\n| `ZYNQ.filteredCount` | Number   | Number of games after current filters                    |\n| `ZYNQ.deviceType`    | Number   | 1 = Desktop (touch), 2 = Desktop (mouse), 3 = Mobile, 4 = Tablet |\n\n### Example\n```javascript\nconsole.log(`Total games: ${ZYNQ.total}`);\nconsole.log(`Showing: ${ZYNQ.filteredCount} games`);\nconsole.log(`Device type: ${ZYNQ.deviceType}`);\n```"
        },
        {
          "id": "methods",
          "label": "Methods",
          "icon": "cpu",
          "content": "# Methods\n\n### Essential Methods\n\n* `search(query)` → Filter by name or alias\n* `random(limit)` → Get random games (default limit = 1)\n* `getRandomOne()` → Shortcut for one random game\n* `shuffle()` → Shuffle current list\n* `sortBy(key)` → Sort by name, category, alias, etc.\n* `sortByPopularity()` → Sort by playCount (if available)\n* `filterSupported()` → Show only supported games\n* `filterFavorites()` → Show only favorites\n* `getByAlias(alias)` → Find single game by alias\n* `getByCategory(category)` → Filter by category\n* `reset()` → Reset all filters and return to full list\n* `toggleFavorite(alias)` → Add or remove from favorites\n* `isFavorite(alias)` → Check if game is favorite\n\n### History Methods (new in v7.5.28)\n* `addToHistory(alias)` → Add game to history (max 50)\n* `getHistory()` → Return games from history\n* `clearHistory()` → Clear history\n\n### Chaining Example\n```javascript\nconst games = ZYNQ\n  .search('sonic')\n  .filterSupported()\n  .shuffle()\n  .sortBy('name')\n  .list;\n```"
        }
      ]
    },
    {
      "title": "Data",
      "items": [
        {
          "id": "data-structure",
          "label": "Data Structure",
          "icon": "file-text",
          "content": "# Game Object Structure\n\nEvery game in `ZYNQ.list` has the following structure:\n\n```javascript\n{\n  name: \"Super Mario Bros\",\n  category: \"Platformer\",\n  alias: \"mario\",\n  url: \"platformer/mario\",\n  thumb: \"https://cdn.../platformer/mario.webp\",  // 400x300px\n  devices: [2, 3, 4],\n  isSupported: true,     // getter\n  isFavorite: false      // getter\n}\n```\n\n`isSupported` and `isFavorite` are dynamic getters that update automatically."
        }
      ]
    },
    {
      "title": "Usage",
      "items": [
        {
          "id": "examples",
          "label": "Usage Examples",
          "icon": "code",
          "content": "# Practical Examples\n\n### Get one random supported game\n```javascript\nconst game = ZYNQ\n  .filterSupported()\n  .random()\n  .list[0];\n\nconsole.log(`Play: ${game.name}`);\n```\n\n### Get 5 random games from a category\n```javascript\nconst racingGames = ZYNQ\n  .getByCategory('racing')\n  .random(5)\n  .list;\n```\n\n### Shuffle current list\n```javascript\nZYNQ.shuffle();\nconsole.log(ZYNQ.list);\n```\n\n### Favorites + Random\n```javascript\nconst favPicks = ZYNQ\n  .reset()\n  .filterFavorites()\n  .random(3)\n  .list;\n```\n\n### Add to history after playing\n```javascript\nZYNQ.addToHistory(game.alias);\nconst recent = ZYNQ.getHistory();\n```"
        }
      ]
    },
    {
      "title": "Advanced",
      "items": [
        {
          "id": "device-detection",
          "label": "Device Detection",
          "icon": "smartphone",
          "content": "# Device Detection\n\nZYNQ Core automatically detects the device type for compatibility filtering.\n\n### Device Type Values\n\n| Value | Type                      |\n|-------|---------------------------|\n| 1     | Desktop with touch        |\n| 2     | Desktop with mouse/keyboard (default for most PCs) |\n| 3     | Mobile phone              |\n| 4     | Tablet / large touch      |\n\n### Example\n```javascript\nif (ZYNQ.deviceType === 3 || ZYNQ.deviceType === 4) {\n  console.log('Touch device detected');\n}\n```"
        },
        {
          "id": "favorites",
          "label": "Favorites System",
          "icon": "star",
          "content": "# Favorites System\n\nFavorites are automatically saved in **LocalStorage** and persist between sessions.\n\n### Basic Usage\n```javascript\nZYNQ.toggleFavorite('mario');           // toggle\nconsole.log(ZYNQ.isFavorite('mario'));  // true or false\n```\n\n### Get all favorite games\n```javascript\nconst favorites = ZYNQ\n  .reset()\n  .filterFavorites()\n  .list;\n```\n\n### Random favorite game\n```javascript\nconst game = ZYNQ\n  .reset()\n  .filterFavorites()\n  .getRandomOne();\n```\n\n### Combined example\n```javascript\nconst picks = ZYNQ\n  .search('kart')\n  .filterFavorites()\n  .random(2)\n  .list;\n```\n\nFavorites update instantly and work together with all other filters and methods."
        }
      ]
    }
  ]
}
